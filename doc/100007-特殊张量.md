# 特殊张量

```java
import tensorflow as tf
import numpy as np
# 创建全为0的张量
# tf. zeros(维度)
a = tf.zeros([2,2])
a
# 创建全为1的张量
# tf. ones(维度)
b = tf.ones([2,2])
b
# 创建全为指定值的张量
# tf. fill(维度，指定值)    
c = tf.fill([2,2],9)
c      
```

